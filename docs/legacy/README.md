# Legacy App Archive

`legacy_mvp_app.py` is archived here as historical reference only.

- It is not part of the supported PSI runtime.
- Current runtime entrypoints are the FastAPI/ASGI app surfaces under `psi/web/`.
- The shim at `psi/legacy_mvp_app.py` exists only to prevent accidental reuse of the archived module.

