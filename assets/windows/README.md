# Windows Desktop Icon Asset Contract

Windows-user release/update packages must include:

- `assets/windows/psi_desktop_icon.ico`

This icon is a packaged runtime asset for desktop shortcut creation/refresh.
It must not be fetched from an external URL at runtime.
