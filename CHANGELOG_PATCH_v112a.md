## v1.1.2a (tiny overlay)

- Add `scripts/install_anarci.sh` helper to install ANARCI into the active virtualenv via `setup.py`
  (more reproducible than pip wheel builds for ANARCI).
- Update `requirements-heavy.txt` to document the script-based ANARCI install path.
- Add `README_HEAVY_COMPUTE.md` with core vs heavy install instructions.
